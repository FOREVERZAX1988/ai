# Macan 方案B：统一口径重拟合 + 视觉/雷达距离差核算 (2026-09-10 会话三)

> 接 `MLB_MACAN_CROSSBRANCH_SCAN_0910.md`。用户指示：**走B** —— 用干净"同目标"帧重新拟合一条修正时距直线
> （替换现在硬编码在 radar_interface/radard 里的新线性公式），并核算**拟合后视觉与雷达的距离差还剩多少**。
> 脚本：`ai/tools/fit_planB_residual_0910.py`；数据：`/tmp/rows_000000{2,3,4,49,71,72}.npy`（统一口径 bus2, stride=3）；
> 拟合结果：`/tmp/planB_fit.json`；日志：`/tmp/planB.log`。

## 0. 口径与门控（本轮唯一口径，与旧稿的差别在此）
- 载入列：`[idx(bus2), v_use(轮速优先), d_vis, v_vis, v_can(ACC_04), zl(ACC_Gesetzte_Zeitluecke), d_old153, d_new]`
- 门控（**不含任何距离项，避免用距离做门控造成自洽**）：`v≥8 m/s` 且 `|v_vis − v_can| ≤ 0.5 m/s` 且 `100 ≤ idx ≤ 560`。
  旧稿门控里多带了 `|Δd_vis|<3 m`，那一条会**把截距往 0 拉**（把"大残差帧"提前剔掉了），本轮剔除该偏差。
- 通过率：全部 113,750 帧 → **15,040 干净帧（13.2%）**；旧分支 11,011（0002/3/4/49）、新分支 4,029（0071/0072）。
  档位：zl=3 共 4,244 帧、zl=4 共 10,796 帧（0002/0049 **全 zl=4**；0071 **全 zl=3**）。

## 1. 方案B 拟合结果：`t(idx) = a·idx + b`（t = d_vis / max(v,5)）
| 变体 | 拟合集合 | a | b (s) | RMS |
|---|---|---|---|---|
| **B1 池化全部** | 6 route 干净帧 | **0.008969** | **+0.332** | 0.61 s |
| B2 仅新分支 | 0071+0072 | 0.009384 | +0.585 | 0.73 s |
| B3 仅旧分支 | 0002/3/4/49 | 0.008171 | +0.378 | 0.48 s |
| B4 zl=3 | 全部 zl3 | 0.008741 | +0.727 | 0.69 s |
| B4 zl=4 | 全部 zl4 | 0.008414 | +0.333 | 0.52 s |
| 参考：旧153表 | — | 0.008951 | +0.163 | — |
| 参考：代码新线性（b018e1dc） | — | 0.008718 | **+1.018** | — |
| 参考：上轮跨分支最优 | — | 0.008617 | +0.401（含距离门控） | — |

**关键点：干净同目标数据支持的总截距是 +0.33 s（池化）/ +0.59 s（仅新分支），不支持代码里的 +1.02 s。**
新线性公式相对旧153表"多出来的 ~0.86 s 常数时距"，在干净数据里只剩 **+0.17 s（B1）~+0.42 s（B2）**。
→ 即：**方案B ≈ 旧153表（斜率几乎相同、截距差 0.17 s）**，而不是新线性公式。

## 2. 视觉 − 雷达 距离差（核心答案）

### 2.1 全部分支池化（N=15,040）
| 雷达距离口径 | med(视觉−雷达) | med\|Δ\| | ≤5m 占比 | ratio>30%（A2 原厂替换） |
|---|---|---|---|---|
| 旧153表 | +1.01 m | 1.76 m | 77.4% | 8.3% |
| **代码新线性** | **−7.10 m** | 7.35 m | 17.5% | **35.3%** |
| **B1（方案B，池化拟合）** | **−0.77 m** | **1.90 m** | **82.0%** | **6.3%** |
| B3（旧分支拟合） | +0.36 m | 1.99 m | 78.6% | 8.1% |
| **B4（按档位选表）** | **−0.45 m** | **1.68 m** | **82.8%** | **6.2%** |
| B2（仅新分支拟合） | −4.16 m | 4.48 m | 55.6% | 9.1% |

### 2.2 分支内（B2 是样本内、B1 是折中）
| 集合 | 旧153表 | 新线性 | B1 池化 | B2 新分支拟合 |
|---|---|---|---|---|
| 新分支 0071/0072 (n=4029) | +4.15 m / 58.1% | −4.23 m / 46.8% | +2.05 m / 72.1% | −1.74 m / 71.3% |
| 旧分支 0002/3/4/49 (n=11011) | +0.56 m / 84.4% | −7.98 m / 6.8% | −1.25 m / 85.7% | −4.80 m / 49.9% |
（格式：med(视觉−雷达) / ≤5m 占比）

**这就是"拟合 B 之后视觉与雷达差多少"的正面回答：**
- **B1（统一一条线）**：偏差中位 **0.8 m 以内**，中位绝对差 **1.9 m**，82% 帧在 5 m 内 —— 视觉与雷达基本对齐；
- **B4（按档位两条线）**：更优（中位 **0.45 m**、中位绝对差 **1.68 m**、82.8%）；
- 作为对照，**代码现状（新线性）偏差 −7.1 m、仅 17.5% 帧在 5 m 内、A2 会 35% 走"原厂替换"**。

### 2.3 B1 的分段残差（视觉距离分段）
| 段(m) | d_vis | d_B1 | Δ(med) | 相对 |
|---|---|---|---|---|
| 0-15 | 12.6 | 13.8 | −1.01 | 7.3% |
| 15-25 | 19.8 | 21.4 | −1.38 | 9.9% |
| 25-40 | 32.5 | 32.2 | −0.12 | 7.0% |
| 40-60 | 45.8 | 43.0 | +2.10 | 7.9% |
| 60-90 | 71.3 | 70.8 | −1.51 | 7.0% |
| 90+ | 97.2 | 91.2 | +5.22 | 4.8% |
→ **残差全段 5~10%，正负交替、无单调弯曲** ⇒ 支持"原厂不分段、整段平滑"（与 H3 一致），
同时也说明 **B1 无系统性尺度倾斜**（不像新线性那样全段一律偏远 6~12 m）。

## 3. 逐 route（B1 vs 两个对照）
| route | n | 旧153表 | 新线性 | **B1** |
|---|---|---|---|---|
| 0002 | 3535 | +0.21 / 81.1% | −7.42 / 9.3% | −1.51 / 80.3% |
| 0003 | 229 | −0.47 / 87.3% | −8.78 / 6.6% | −2.76 / 79.0% |
| 0004 | 5471 | +0.89 / 85.9% | −8.43 / 6.9% | −0.91 / **90.8%** |
| 0049 | 1776 | +0.07 / 86.3% | −7.87 / 1.8% | −1.63 / 81.5% |
| 0071 | 2157 | +3.40 / 64.9% | −5.43 / 37.5% | +1.32 / 78.9% |
| 0072 | 1872 | +4.79 / 50.2% | −3.13 / 57.5% | +2.76 / 64.2% |
→ B1 在 6 条 route 上**全部**优于新线性；与旧153表相比，旧分支略差（−1~−2.7 m vs +0.2~+0.9 m），
**新分支明显更好**（+1.3/+2.8 m vs +3.4/+4.8 m）——这正是"旧表是半年前视觉口径"的体现。

## 4. 跨分支泛化的真实缺口（必须记住）
- 用**新分支**拟合的 B2 拿到**旧分支**上用：med −4.80 m、仅 49.9% ≤5 m。
- 用**旧分支**拟合的 B3 拿到**新分支**上用：med +3.95 m、62.4% ≤5 m。
→ 两条分支存在 **±4 m（≈15%）
** 的真实互不通用性；任何单一公式都只能折中（B1 两条分支残差 −1.25 / +2.05 m）。
这也再次解释了过去"公式反复横跳"的根因：**每次换基准集合，最优公式就换一个截距。**

## 5. 结论与建议（可执行）
1. **方案B 成立且优于现状**：B1 `t = 0.008969·idx + 0.332`（或按档位的 B4）把"视觉−雷达"中位差压到
   **0.5~0.8 m、中位绝对差 1.7~1.9 m、≤5 m 占比 82%**，A2 原厂替换率回到 **6%** —— 视觉纠偏能力保留。
2. **不要保留 +1.02 s 截距**：干净同目标数据（不含距离门控、跨 6 条 route）给出的截距是 +0.33 s。
   代码现状的 `0.008718/1.0178` 使中位差 −7.1 m、A2 替换 35%，应替换。
3. **优先 B1（简单、单表）**；若接受 10% 档位修正，用 **B4（zl3: 0.008741/0.727，zl4: 0.008414/0.333，用 ACC_Gesetzte_Zeitluecke 选表）**，
   收益 +0.3 m 中位差、替换率 6.3%→6.2%，代价是多一张表。
4. **仍缺绝对真值**：B 是"以视觉为基准"拟合出来的，其 ≈0 残差**部分是自洽而非独立验证**。
   绝对尺度裁定仍需**强相对运动路段**（接近/远离、跟停起步）的运动学闭合 ∫(v_lead−v_ego)dt，
   或跟停末距/Zetluecke 档位锚点。**在拿到之前，B 的价值是"与 openpilot 主 lead 传感器自洽 + 融合不再丢视觉"。**
5. **顺带必须修**：idx<100 硬锚 0.8 s（idx=100 处 0.8→1.89 s 突跳）、idx>560 硬截顶 6.0 s。
   B1/B4 的直线形式可直接替换这两处人为分段。
6. 0072 依旧最噪（B1 下 med +2.76 m、仅 64% ≤5 m），标定应降权；0071 是最干净的新分支样本。

---

## 6. 「距离反复横跳」专项检验（同轮，324,589 帧 / 4 route，全段连续帧）

> 用户追问：**走B（拟合 B 公式）之后，距离还会反复横跳吗？**
> 口径：`old153` / `newcode`（代码现状 0.008718/+1.0178，含 `idx<100→0.8s`、`idx>560→6.0s` 伪分段）/
> `newline`（同斜率纯直线）/ `B1`（0.008969/+0.332）/ `B4`（zl3 0.008741/+0.727｜zl4 0.008414/+0.333）。
> 工具：`ai/tools/dump_rows_cont_bus2.py`（10 列，含 seg,k 以判"帧连续"）、
> `ai/tools/check_jitter_planB_0910.py`（A2 逻辑复刻），日志 `/tmp/jitter_planB.log`。
> 数据：0004(176,853) 0071(53,310) 0072(39,678) 0049(54,748)，连续帧对 **324,029**。

### 6.1 帧间抖动：与公式无关（B 不会更"横跳"）

| 口径 | 池化 med\|Δd\| | P99 | 每千帧 >5m | 每千帧 >10m |
|---|---|---|---|---|
| old153 | 0.00 m | 0.33 m | 0.5 | 0.4 |
| newcode | 0.00 m | 0.25 m | 0.7 | 0.6 |
| newline | 0.00 m | 0.27 m | 0.5 | 0.4 |
| **B1** | **0.00 m** | **0.25 m** | **0.5** | **0.4** |
| **B4** | **0.00 m** | **0.24 m** | **0.5** | **0.4** |

逐 route 同结论（0004/0071/0072/0049 的 med 全为 0.00，P99 0.22~0.44 m）。
**抖动 ≈ 斜率 × v × Δidx + 视觉噪声**，与截距/口径无关 ⇒ 换 B 不引入、也不放大帧间跳变。
（仅有 `newcode` 在 0004 平滑帧上多出 0.3 事件/千帧，来源即下节的伪分段。）

### 6.2 真正会造成"跳"的是代码里两处**伪分段**（B1/B4 自动消除）

| route | idx 跨 100 帧对 | newcode 跨步中位 | newline/B1/old153 跨步中位 |
|---|---|---|---|
| 0004 | 72 | **14.1 m** | 0.1 m |
| 0071 | 4 | **22.9 m** | 10.2 / 9.9 / 10.1 m（同帧对，idx/v 本身变化大） |
| 0049 | 7 | **17.0 m** | 6.5 / 6.6 / 6.8 m |
| 0072 | 0 | — | — |

- `idx<100→0.8s` 与线性式在 idx=100 给出 1.89s 冲突 ⇒ **1.09 s 阶跃**（≈20 m/s 下 22 m）：同一批跨界帧上
  `newcode` 比纯直线多跳 **13~14 m**。**这才是"距离反复横跳"的直接元凶。**
- `idx>560→6.0s` 实际在 idx>571 才生效（阶跃很小，0004 中位 0.1 m），但会把真实 ~7 s 饱和段**截短 0.7~1.0 s**；
  而 0071/0072 有 **13.8% / 16.6% 帧处于 idx>560** ⇒ 属**系统性偏差**（不是抖动），同样应去掉。
- B1/B4 是一条（或按档位两条）直线，**没有人为分段**，两处跳变同时消失。

### 6.3 A2 模式翻转 = 融合层"横跳"的真实来源（B 把它降到旧表以下）

A2 复刻（ratio 在 idx 域、w 0.7→1.0 连续、dist_factor 分段、视觉权重上限 0.5）：

| route | old153 替换% / 翻转每千帧 | newcode | B1 | B4 |
|---|---|---|---|---|
| 0004 | 11.0 / 6.7 | **70.8 / 10.4** | 7.6 / **5.2** | 8.6 / 6.1 |
| 0049 | 8.4 / 4.6 | **65.9 / 12.0** | 8.8 / **5.4** | 8.5 / 5.4 |
| 0071 | 28.7 / 20.0 | 18.3 / 15.8 | 20.5 / 15.4 | **13.0 / 11.3** |
| 0072 | 29.6 / 20.4 | 21.5 / 12.5 | 22.2 / 13.7 | **19.4 / 12.8** |
| **池化** | 15.8 / 10.2 | **55.3 / 11.8** | 11.7 / **7.9** | 10.6 / **7.7** |

- 旧分支（0004/0049 高速旧数据）：`newcode` 把 ratio 系统性推高（偏差 −6.3 m ⇒ 替换 **55%**），
  大量帧在 30% 阈值附近**来回穿越**（10.4/12.0 每千帧）＝ 视觉权重在 0.7↔1.0 间反复摆动，即用户看到的"横跳"。
  **B1/B4 把它压到 5.2/5.4 每千帧、替换 8%**——比旧表还稳。
- 融合后距离本身的帧间抖动：各口径 med 0.01 m、P99 0.6~1.1 m，差异很小 ⇒ "横跳"**不是融合输出噪声，而是替换/混合模式的高频切换**。
- 新分支（0071/0072）**天生翻转率高**（12.5~20.4 每千帧，B4 最低），主因是 **OP 在控时的目标切换 + 视觉噪声**；
  换公式只能改善约 20~30%，要进一步降需动 **A2 门控**（目标切换检测 / ratio 时序中值），非换表可解。

### 6.4 本轮结论（两问齐答）

1. **走 B 之后视觉与雷达的距离差**（干净同目标集）：B1 中位 **−0.77 m**、中位绝对差 1.90 m、**82.0% ≤5 m**；
   B4 更优（−0.45 m / 1.68 m / 82.8%）；对照 `newcode` 为 **−7.10 m / 7.35 m / 17.5%**。
2. **走 B 之后不会新增"反复横跳"**：B1/B4 的帧间抖动与旧表/新公式完全同级（med 0.00 m、P99 0.25 m），
   而且**消掉了代码现状 idx=100 处 14~23 m 的阶跃**，并把 A2 替换/模式翻转降到旧表以下（池化 55.3%→11.7%、11.8→7.9 每千帧）。
3. **仍未决**：B 以视觉为基准拟合，其 ≈0 残差部分是自洽；绝对尺度仍需**强相对运动路段**的
   `∫(v_lead−v_ego)dt` 闭合或跟停末距锚点。0072 噪声大（替换 19.4%、翻转 12.8 每千帧），标定应降权。
4. **可执行的下一步**：在 `radar_interface.py`（A3）与 `radard.py::_macan_t_from_idx/_macan_drel_to_idx`（A2）**同步**改成 B1（单表，优先）
   或 B4（按 `ACC_Gesetzte_Zeitluecke` 选表），**并删除 `idx<100→0.8s`、`idx>560→6.0s` 两处伪分段**；
   随后按 §2.3 分段残差重标 A2 的 ratio 阈值/`dist_factor`。

---

## 7. 落地（2026-09-10，代码已改：B1 单表）

| # | 文件 | 位置 | 改动 |
|---|---|---|---|
| A3 | `opendbc_repo/opendbc/car/volkswagen/radar_interface.py` | `RadarInterface._update_macan` | `t = MACAN_B1_T_A*idx + MACAN_B1_T_B`；**删除** `idx<100 -> 0.8 s`、`idx>560 -> 6.0 s` 两处伪分段（原 `idx=100` 处 `0.8 -> 1.89 s` 突跳 = 1.09 s/idx；现全段直线，逐 idx 最大 Δt = 0.00897 s） |
| A2 | `openpilot/selfdrive/controls/radard.py` | `_macan_t_from_idx` / `_macan_idx_to_drel` / `_macan_drel_to_idx` | 同一组常量；反解改为 `clip((t - B)/A, 1, 1020)`，**删除** `t<=0.8 -> 100` / `t>=6.0 -> 561` 硬锚（两端阶梯与 A3 不一致的来源） |
| — | 两文件模块顶层 | `MACAN_B1_T_A = 0.008969` / `MACAN_B1_T_B = 0.332` | 跨仓库无法共享常量，故各留一份；复核脚本强制断言 A2 == A3 |

**复核工具（新增，保留）**：`ai/tools/verify_planB_code_0910.py`
- 不硬编码系数：直接从两个源文件正则提取 `MACAN_B1_T_A/B`，先做同源断言，再在 `/tmp/rows_*.npy` 干净同目标集上复现指标。
- 实测（2026-09-10）：同源 True；池化 **−0.76 m / 1.89 m / 82.0% ≤5 m / A2 替换率 6.3%** —— 与本文 §2.1 基线（−0.77 / 1.90 / 82.0% / 6.3%）一致。
- 逐 route 与 §3 一致（B1 在 6 条 route 上全部优于 newcode）。

**变更幅度**（同一 idx，v=20 m/s 口径）：idx=100 −35.0%、idx=200 −23.0%、idx=300 −16.8%、idx=400 −13.0%、idx=560 −9.2% —— 即把"新线性 +1.02 s 截距"造成的系统性偏远收回。

## 8. 已知遗留：第三个映射（仪表显示源换算）尚未统一 —— 待决

`opendbc/car/volkswagen/carcontroller.py::CarController.op_lead_to_index`（被 `:505` 用于**仪表车距显示源的 30% 迟滞比较**）仍在用 **0902 单调表 `_MACAN_ABSTAND_T_MONO`（153 点、idx 27~1021）** 做 `t -> idx` 反解：

- 该表与 A2/A3 **本来就不一致**（A2 用 newcode、它用 0902 表），本次改动**没有让它变差，也没有修好**；
- 数值差异：idx=400 时 0902 表 t=3.128 s，B1 为 3.92 s（+25%）；idx=780 时 7.149 vs 7.33 s；
- 风险：`vis_raw` 与 `lead_distance` 不在同一尺度上，30% 迟滞判断被系统偏置污染 —— 这正是 2026-08-25 修过的"两源在边界来回切"同源问题；
- 但该段代码附带大量硬挣来的仪表修复（显示变化率限速、无目标透传 0、`lead_hold` 保持窗），动它需要单独验证。
- **建议**：下次可用 `vis_raw = A2._macan_drel_to_idx`（B1 反解）替换 `op_lead_to_index` 的表，使显示源比较落在同一尺度；先仅改这一处、单独一条 commit，便于回退。

## 9. 落地后的两层验证（2026-09-10）

| 工具 | 层次 | 结果 |
|---|---|---|
| `ai/tools/verify_planB_code_0910.py` | 数据级（6 route 干净同目标集 15,040 帧） | 从**源码常量**提取 A2/A3 → 同源断言 True；B1 池化 **−0.76 / 1.89 / 82.0% / 6.3%**，与 §2.1 基线一致；逐 route 与 §3 一致 |
| `ai/tools/smoke_macan_a3b1_0910.py` | 代码路径级（合成 bus2 帧喂真身 `RadarInterface._update_macan`） | idx 1..1020 与 B1 直线一致（相对 1e-6）；**逐 idx 最大 dRel 步长 = 0.1794 m = 直线斜率**（旧代码 idx=100 处 1.09 s 突跳已消失）；idx=0 / 1021 仍正确回落视觉；A2 真身反解往返最大误差 0 idx |

**关于「还会不会反复横跳」**：本次改动只把**静态映射**由分段折线换为直线，因此
- §6.1 的帧间抖动结论不变（抖动来自原厂 idx 本身与视觉噪声，与公式无关：B1 med 0.00 m / P99 0.25 m，与旧表同级）；
- 新增消除的是**静态不连续**：A3 的 `idx<100/idx>560` 折点、A2 的 `100/561` 硬锚，两者都是"同一条真实曲线被切了三段"造成的**固定位置阶跃**（每次 idx 扫过 100/560 就跳一次）——这才是"反复横跳"里可被公式解释的那部分，现已归零。

