{
  "namespace": "sandbox",
  "revision": 1,
  "fields": {
    "ai_sandbox_shell": {
      "type": "boolean",
      "default": true,
      "label": "Enable shell sandbox",
      "description": "Whether shell command tools run through the confined sandbox."
    },
    "ai_sandbox_mode": {
      "type": "enum",
      "default": "read-only",
      "enum": ["read-only", "confined", "isolated", "disabled"],
      "label": "Sandbox mode",
      "description": "Default containment policy for code and shell execution."
    },
    "ai_sandbox_timeout": {
      "type": "number",
      "default": 60,
      "min": 1,
      "max": 3600,
      "label": "Sandbox timeout (s)",
      "description": "Maximum wall-clock time for a single sandboxed execution."
    }
  }
}
